import { Dialog, styled } from "@mui/material";

export const CompanyJoinOrganizationWrapper = styled(Dialog)`
  & .MuiDialogContent-root {
    width: 500px;
    & .modal_title {
      margin-bottom: 6px;
    }
  }
`;
